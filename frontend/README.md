# Frontend (Next.js)

## Run

```bash
cd frontend
npm install
npm run dev
```

App runs on `http://localhost:3002`.

## Backend URL

Optionally set:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:8002
```

The chat UI streams from `POST /chat/stream`.
